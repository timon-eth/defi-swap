/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
    domains: ['raw.githubusercontent.com', 'coin-images.coingecko.com', 's2.coinmarketcap.com', 'assets.coingecko.com'], // Add the domain here
  },
}

module.exports = nextConfig
